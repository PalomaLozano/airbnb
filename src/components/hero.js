import imgHero from '../images/Group77.png';

const Hero = () => {
  return (
    <div className="hero">
      <img src={imgHero} alt="group of images" />;
    </div>
  );
};

export default Hero;
